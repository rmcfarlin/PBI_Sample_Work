# Power BI Project local/cache artifacts
.pbi/
*.pbix.tmp
**/.pbi/localSettings.json
**/.pbi/cache.abf

# Local Claude Code config — personal, not for the public repo
.claude/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*~

# Secrets / local config — never publish
*.env
*.secret
**/connection-strings.json
**/credentials.json
